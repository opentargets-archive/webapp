describe('Service: cttvServices', function () {
    "use strict";
    // Load the services
    // beforeEach (module('cttvServices'));

    // describe ('otAppToApi', function () {
    // 	var searchService;
    // 	beforeEach (inject(function (otAppToApi) {
    // 	    searchService = otAppToApi;
    // 	}));
	// it ("returns a 'search' object", function () {
	//     assert.isObject(searchService);
	// });

	// describe ('createSearchInitObject', function () {
	//     it ("has the createSearchInitObject method", function () {
	// 	assert.isDefined (searchService.createSearchInitObject);
	// 	assert.isFunction (searchService.createSearchInitObject);
	//     });
	//     it ("returns the 'search' object", function () {
	// 	var search = searchService.createSearchInitObject();
	// 	assert.isDefined (search);
	// 	assert.isObject (search);
	//     });
	//     it ("returns a 'search' object with empty results", function () {
	// 	var search = searchService.createSearchInitObject();
	// 	assert.isObject(search.results);
	// 	assert.equal(Object.keys(search.results).length, 0);
	//     });
	//     it ("returns a 'search' object with a query field", function () {
	// 	var search = searchService.createSearchInitObject();
	// 	assert.isObject(search.query);
	// 	assert.isDefined(search.query.q);
	// 	assert.isDefined(search.query.page);
	// 	assert.isDefined(search.query.size);
	//     });
	// });

	// describe ('getApiQueryObject', function () {
	//     it ("has the getApiQueryObject", function () {
	// 	assert.isDefined (searchService.getApiQueryObject);
	//     });
	// });
    });

});
describe('Ctrl: cttvControllers', function() {
    // Load the module with MainController
    beforeEach(module('cttvControllers'));
});
